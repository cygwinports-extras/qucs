diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x ltmain.sh -x 'config.*' -x depcomp -x install-sh -x missing -x mkinstalldirs -x autom4te.cache -x '*compile' -x Makefile.in.in -x 'intltool*.in' -x 'xml-i18n-*.in' -x '*.pyc' -x '*.mo' -x '*.gmo' -x ABOUT-NLS -x Makevars.template -x COPYING -x INSTALL -x mdate-sh -x '*.orig' -x '*.rej' -x '*~' -x '*.temp' -x texinfo.tex -x ylwrap -x Makefile.in -x makefile.in -x configure -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x omf.make -x xmldocs.make origsrc/qucs-0.0.10/configure.ac src/qucs-0.0.10/configure.ac
--- origsrc/qucs-0.0.10/configure.ac	2006-08-31 12:27:29.000000000 -0500
+++ src/qucs-0.0.10/configure.ac	2006-12-12 20:55:26.111375000 -0600
@@ -82,7 +82,6 @@
 
 dnl Release specific. Uncomment these as required.
 AC_CONFIG_SUBDIRS(qucs-core)
-# AC_CONFIG_SUBDIRS(freehdl)
 RELEASEDIRS="qucs-core"
 # RELEASEDIRS="$RELEASEDIRS freehdl"
 AC_SUBST(RELEASEDIRS)
@@ -218,7 +217,7 @@
 
 dnl Check for path transformation.
 case $build_os in
-mingw* | cygwin*)
+mingw*)
   PATHXFORM="cygpath -w"
   ;;
 *)
